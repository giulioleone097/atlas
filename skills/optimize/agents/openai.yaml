interface:
  display_name: "Optimize"
  short_description: "Improve a measured metric while preserving guard limits"
  default_prompt: "Use $optimize with the stated metric and scope. Measure the baseline, compare bounded hypotheses, preserve guard limits and retain only verified improvements beyond noise."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
