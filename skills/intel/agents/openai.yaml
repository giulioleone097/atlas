interface:
  display_name: "Intel"
  short_description: "Research a question and produce a cited evidence brief"
  default_prompt: "Use $intel to research this question through primary sources, verify changing claims and produce one concise cited brief. Delegate only useful independent reading when available; keep bounded research local and preserve explicit model and output constraints."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
