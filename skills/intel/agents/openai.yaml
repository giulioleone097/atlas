interface:
  display_name: "Intel"
  short_description: "Research a question and produce a cited evidence brief"
  default_prompt: "Use $intel to research this question through primary sources, delegate the reading, and produce one Markdown brief with citations for its findings."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
