interface:
  display_name: "Build"
  short_description: "Implement, fix and verify the requested code change"
  default_prompt: "Use $build to complete this change. Keep small work local, plan or delegate only when useful, fix the shared cause, review the result and run the relevant proof."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
