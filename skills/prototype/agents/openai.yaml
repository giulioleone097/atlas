interface:
  display_name: "Prototype"
  short_description: "Test a design with disposable logic or UI prototypes"
  default_prompt: "Use $prototype to test this design question with a disposable interactive artifact. Capture my verdict and use the result to guide the next design or implementation step."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
