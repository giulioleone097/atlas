interface:
  display_name: "Ship"
  short_description: "Deliver verified changes or prepare a PR description"
  default_prompt: "Use $ship for the delivery steps already requested. Commit verified changes, push or open a PR when authorized, and handle a dossier-only request without committing unrelated work."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
