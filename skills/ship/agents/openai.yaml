interface:
  display_name: "Ship"
  short_description: "Ship with concise impact, risk and ownership dossier"
  default_prompt: "Use $ship for authorized delivery with a concise dossier by default, including local-only work. Cover domain/file drilldown, cross-domain impact, material risks and CODEOWNERS when present. Resolve the governing owner policy separately from approvals; group repeated details. Explicit no-dossier overrides presentation only. Publish only within authority and read back the result."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
