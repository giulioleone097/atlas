interface:
  display_name: "Ship"
  short_description: "Deliver verified changes or prepare a PR description"
  default_prompt: "Use $ship for the authorized delivery steps. Every requested PR creation or body update gets an exact-BASE/HEAD dossier with domain and file drilldown, complete changed-path accounting and truthful proof/gaps by default. Preserve author text, publish once and read back the body and head. Combined PR/dossier flags continue through delivery; dossier-only requests stop at preparation. Link retained optimization checkpoints and return actual PR/CI evidence to the native parent owner without prematurely completing it."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
