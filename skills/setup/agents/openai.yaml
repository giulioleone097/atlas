interface:
  display_name: "Setup"
  short_description: "Configure Atlas project rules or refresh its repository map"
  default_prompt: "Use $setup for the requested project operation. Install or remove instructions only when explicitly requested; for implicit discovery use $setup --map and leave project instructions untouched."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
