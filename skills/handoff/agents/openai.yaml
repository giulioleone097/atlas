interface:
  display_name: "Handoff"
  short_description: "Save unfinished work for a reliable next session"
  default_prompt: "Use $handoff to preserve this unfinished work, its verified results, decisions, artifacts and blockers, with an exact next action for resumption."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
