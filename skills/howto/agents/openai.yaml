interface:
  display_name: "Howto"
  short_description: "Map dependent decisions across multiple sessions"
  default_prompt: "Use $howto to map this initiative, identify decision dependencies, resolve the next available decision and save the remaining map for resumption."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
