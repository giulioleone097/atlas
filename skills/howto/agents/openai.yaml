interface:
  display_name: "Howto"
  short_description: "Chart the path from an objective to verified work"
  default_prompt: "Use $howto to establish this objective and its proof, map dependencies, resolve ready decisions, retain evidence and checkpoints, and continue authorized work or preserve the exact next step for resumption."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
