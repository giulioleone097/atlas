interface:
  display_name: "Scope"
  short_description: "Verify incoming work and define its outcome and proof"
  default_prompt: "Use $scope to read the source, establish the outcome and acceptance check, resolve material unknowns and continue into implementation unless I requested assessment or a card only."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
