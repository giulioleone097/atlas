---
name: reverse
description: Use when reverse engineering achieved development results into transferable causal principles and a reusable decision discipline, grounded in the strategies that worked.
argument-hint: "[result or PR] [--drilldown | --kit]"
---

1. Bind the achieved result and available evidence to the request. Read `<plugin root>/core/ATLAS.md` if not loaded; <plugin root> is the parent of skills/. Preserve scope and authority. A future target alone is missing outcome evidence.
2. Run `<this skill>/references/analyze.md`; <this skill> is this directory. Reuse current evidence and trace every material mechanism/practice, its dependencies and limits. This supporting analysis is not the default user-facing output.
3. Run `<this skill>/references/playbook.md` to derive transferable causal principles from the supported strategies: identify why each intervention worked, abstract beyond its technical means, and challenge the rule in another domain. Deliver actionable principles with their decisive conditions; a short technique list is insufficient.
4. Pass the principles and supporting receipt to `<plugin root>/skills/ship/references/learn.md` for authorized ownership, integration and reuse. Case implementation is not future adoption; familiar practices still reach the directive. Internal receipts do not expand the public response.

Use concise, navigable causal drilldown as the intermediate human layer when useful. Always finish with self-contained transferable operating principles, including after expanded analysis or assets; no inventory or status narration follows it. Preserve decisive qualifiers. A minimal-only request skips the human layer, preserving complete actionable principles. --drilldown expands graph/evidence/limits; --kit or an explicit implementations request opens relevant assets and adaptation instructions.

Stop after the transferable principles, requested expansion and authorized learning/read-back, or a precise blocking evidence gap. Preserve the complete supporting practice set without printing a mandatory dossier, kit, table or usage tutorial. No new optimization, commit or release starts here.
