interface:
  display_name: "Reverse"
  short_description: "Deliver working assets and a ready agent brief"
  default_prompt: "Use $reverse to extract the working assets behind this result and deliver an operational agent brief: exact use, adaptation, invariants and checks. Preserve causal drilldown and truthful adoption; prose guidance alone is insufficient."
policy:
  allow_implicit_invocation: true
