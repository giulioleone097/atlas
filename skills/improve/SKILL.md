---
name: improve
description: Use when improving codebase structure or practice from evidence. Reuse validated lessons, reverse achieved results and route the next bounded improvement to simplification, optimization or review.
argument-hint: "[what to make healthier: a direction, a subsystem or a planned change]"
---

1. Classify the ask from outcome and evidence under core's Intent rule. Learning from an achieved result invokes public `<plugin root>/skills/reverse/SKILL.md`; product/outcome evolution invokes `<plugin root>/skills/howto/SKILL.md` with current authority. Shrinking invokes `simplify`; measurable performance/cost/quality invokes `optimize`; an audit invokes `review` in its requested mode. Structural assessment reads `<this skill>/references/deepen.md`; `<this skill>` is this directory. Choose the smallest route that proves the outcome and carry necessary later stages. Ask through `<plugin root>/skills/scope/references/asking.md` only for unresolved user goals or values, never routine classification.

Before dispatch, reuse applicable practices through `<plugin root>/skills/scope/references/context.md`. A learning-only request dispatches reverse directly; that entrypoint owns analysis. When selecting a further improvement from an achieved result, reuse its reverse receipt or run `<plugin root>/skills/reverse/references/analyze.md` to identify the supported mechanism and remaining gap. A hypothesis is a validation need, not an established default.

2. The invoked skill owns execution and proof; consume its existing learning receipt. If it returns materially new verified evidence without one, run `<plugin root>/skills/ship/references/learn.md` within inherited write limits. A read-only result permits only analysis/proposals. Reuse valid proof and return pending learning separately; never start a second improvement just to compound gains. A survey ends as deepen.md directs: report written, then the picked card handed to `atlasme` (Skill tool `atlas:atlasme`, `$atlasme` on Codex). Either way print:

```
dispatch: <the ask in a few words> -> reverse | howto --evolve | simplify | optimize | review --repo | --debt | --rules | deepen survey - <the signal that decided it>
report: <path - survey only>
card: <the pick> -> atlasme | none picked - <survey only>
learning: <applicable practice used; reverse/learning receipt, actual outcome delta or unresolved gap>
```

Stop when the one dispatch has run or the survey report is printed and its pick handed to `atlasme`. One dispatch or one survey per run. A dispatched skill completes its own authorized edits and proof; only the structural survey is read-only, with its proposed change landing later through `scope` -> `build`.
