interface:
  display_name: "Simplify"
  short_description: "Simplify scoped code while preserving its behavior"
  default_prompt: "Use $simplify on the requested code or audit. Reuse existing capabilities, remove demonstrated unnecessary complexity and verify preserved behavior; keep audits read-only."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
